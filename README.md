# claude-desktop
Pour exécuter Claude Desktop via Docker, 
s’appuie sur une image Docker existante pour Claude Code qui permet d’exécuter Claude dans un conteneur.
avec un accès graphique natif à Claude Desktop
